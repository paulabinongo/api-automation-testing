/**
 * **Auto Loan** payment preview — placeholder. Register **`computeAutoLoanPreview`** in
 * **`../computationRegistry.js`** once **`AUTO_LOAN`** is in **`loanProductCatalog.js`** and maths are defined.
 */

/**
 * @param {number} principalCents
 * @param {number} termMonths
 * @param {object} [options]
 * @returns {object | null}
 */
export function computeAutoLoanPreview(principalCents, termMonths, options = {}) {
  void principalCents
  void termMonths
  void options
  return null
}

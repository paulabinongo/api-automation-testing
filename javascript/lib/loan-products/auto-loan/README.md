# Auto Loan (placeholder)

Add **`AUTO_LOAN`** (or your code) here when you implement it:

1. **`../catalog.js`** — products come from **`LOAN_PRODUCTS_BY_CODE`** in **`loanProductCatalog.js`** (add the frozen product object + key).
2. **`../registry.js`** — **`ELIGIBILITY_BY_PRODUCT_CODE`**
3. **`../computationRegistry.js`** — **`COMPUTATION_BY_PRODUCT_CODE`** if you expose a payment preview.
4. **`../lifecyclePolicies.js`** — opt into document / PEP / Metrobank sets only if those gates apply.
5. **`eligibility.js`**, **`validateIntake.js`**, **`sample payloads`** — mirror **`personal-loan/README.md`**.

Until then, this folder is an empty scaffold for documentation and future modules.
